<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.org.downesward.stargate"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="9" android:targetSdkVersion="18" />

    <application
        android:icon="@drawable/ic_stargate"
        android:label="@string/app_name"
        android:allowBackup="true" >
        <activity
            android:name=".ActionSelectionActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WorldViewerActivity" />
        <activity android:name=".KnownWorldSelectionActivity" />
        <activity android:name=".PlotGeneratorActivity" />
        <activity android:name="com.lamerman.FileDialog" >
        </activity>
    </application>

</manifest>